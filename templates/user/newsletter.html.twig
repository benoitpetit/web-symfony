
{{ form_start (form, { 'attr': {'class': 'form-inline'} }) }}

        <div class="col-5 text-center justify-content-center align-items-center d-flex mx-auto ">

{{ form_widget (form, { 'attr': {'class': 'form-control mr-sm-2 email'} }) }}

            <button type="submit" class="btn btn-outline-success">Ok</button>
        </div>

{{ form_end (form) }}

{# <form class="form-inline">
    <div class="col-sm-9">
        <input class="form-control mr-sm-2 email" type="email" placeholder="Ton email">
    </div>
    <div class="col-sm-3 text-center my-2 my-sm-0">
        <button type="submit" class="btn btn-outline-success "> Ok </button>
    </div>
</form> #}

